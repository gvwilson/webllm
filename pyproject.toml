[project]
name = "webllm"
version = "0.1.0"
description = "learning web programming with LLMs"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "htpy>=25.12.0",
    "httpx>=0.27.0",
    "python-fasthtml>=0.6.0",
    "uvicorn>=0.24.0",
    "mccole>=4.0.0",
    "pytest>=8.0.0",
    "ruff>=0.15.12",
    "taskipy>=1.14.1",
]

[tool.taskipy.tasks.bib]
help = "Check bibliography entries."
cmd = "mccole bib"

[tool.taskipy.tasks.clean]
help = "Clean up generated and cache files."
cmd = """
    find . -type f -name '*~' -exec rm {} \\;
    find . -type d -name __pycache__ | xargs rm -rf
    find . -type d -name .ruff_cache | xargs rm -rf
"""

[tool.taskipy.tasks.check]
help = "Check code and project."
cmd = """
    mccole check --src . --dst docs
    typos *.md */*.md
    ruff check .
"""

[tool.taskipy.tasks.serve]
help = "Serve generated HTML."
cmd = "python -m http.server -d docs"

[tool.taskipy.tasks.site]
help = "Build HTML."
cmd = """
    mccole build --src . --dst docs
    touch docs/.nojekyll
"""

[tool.mccole]
brand = "WebLLM"
repo = "https://github.com/gvwilson/webllm"
skips = [
    "**/*~",
    "**/__pycache__/**",
    "docs/**",
    "log/**",
    "*/style.css",
    "uv.lock",
    "*.csv",
    "*.db",
    "*.html",
    "*.py",
    "*.sh"
]
